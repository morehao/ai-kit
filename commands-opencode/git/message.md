---
description: 将中文描述转换为 Conventional Commits 格式的 commit message（纯生成，不提交）
---

# 任务
本命令由 `git-kit` skill 的 **commit-message 分支**实现。

用 `skill` 工具加载 `git-kit`，将本次请求视为 `commit-message` 意图执行。

# 参数
$ARGUMENTS

> `<中文描述>`，必填；若上方参数为空，先向用户索取再生成。
