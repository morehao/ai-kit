{
  "name": "ai-kit",
  "private": true,
  "description": "个人 AI 编码工具扩展集中仓（skills / opencode 指令 / dsh 命令）。仅 commands-dsh 子包发布到 npm，用 changesets 自动管理版本与发布。",
  "packageManager": "pnpm@11.21.0",
  "workspaces": [
    "commands-dsh"
  ],
  "scripts": {
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.28.0"
  }
}
