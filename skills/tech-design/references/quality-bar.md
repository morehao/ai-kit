# 质量红线（quality-bar）

撰写全程遵守。分六块：图、代码、论证、反模式、评审清单、语言。

## Mermaid 图规范（写图前必读）

### 何时用图

| 场景 | 图类型 |
|------|--------|
| 架构/拓扑/调用链概览 | `flowchart TB`（或 BT/LR/RL） |
| 时序/交互 | `sequenceDiagram` |
| 数据模型/ER | `erDiagram` |
| 类/领域关系 | `classDiagram` |
| 状态与流转 | `stateDiagram-v2` |

- 图能说清才放；同一文档**不超过 4-5 张**，避免用图代替论证。
- **只用内嵌 Mermaid**：不引用外链图片（离线打不开、会失效、评审时看不见）。
- 图必须与正文一致：图里出现的每个节点/组件，正文都要有解释；正文的关键路径必须能在图上找到。

### 语法方向（最重要的一条）

Mermaid 语法是 `节点ID[展示文本]`——**方括号里是展示文本，等号/定义左边才是 ID**：

```mermaid
flowchart LR
    client["客户端"] --> apiGateway["API 网关"]
    apiGateway --> userService["用户服务"]
```

规则：

- ID 用**语义化英文**（`client`、`apiGateway`、`orderDB`）；禁止无意义字母（`A`、`B`、`C`）、禁止中文 ID。
- 展示文本用中文可读，**一律用双引号包裹**（含中文标点或空格时不加引号会解析出错）。
- 首次出现完整定义 `id["展示"]`，后续引用只写 `id`（如 `client --> orderDB`）。
- **不要**用旧式反向写法 `客户端[client]`——它把中文当 ID、英文当展示文本，语义颠倒且部分预览器异常。参考旧示例时一律按本节改写。

### 校验

成稿后（本机能定位到本 skill 目录时）跑：

```bash
node <本skill目录>/scripts/check-mermaid.mjs <生成文件>
```

- 全部 `OK` → 通过；`[MERMAID-ERROR]` → **必须修复并重跑，不静默放过**；`[MERMAID-WARN]`（`<br>` 规范提示）为提示级，可忽略或顺手修正。
- 脚本复用 `project-insight` 的校验引擎（同一套 mermaid + jsdom），本 skill 的 `scripts/check-mermaid.mjs` 是薄壳，按自身位置相对定位。依赖缺失时会明确提示（需 `skills/project-insight/scripts/` 已 `npm install`）。

## 代码片段原则

默认**不含代码**，仅在以下情况添加：用户明确要求示例；需要展示新架构模式的典型写法；API 方案需要接口定义/请求响应示例；核心算法需伪代码。

格式：标准 Markdown 代码块 + 语言标注 + 简洁中文注释。示例代表性优先，不要贴大段无关实现；**代码里的密钥、域名、内网地址一律用占位符**。

## 论证充分性

- **量化义务**：涉及性能/容量/可用性/成本的论断，按 `quantification.md` 给数字或区间 + 口径 + 假设来源；给不出就标为「待验证假设」并写清不成立时的应对。形容词（"高性能""海量"）不算论证。
- **取舍对偶**：每个选型给 ≥2 个方案，并回答三问——为什么选它、代价是什么、**什么情况下不该选它**。只列优点或只给一个方案视为未完成。
- **可证伪**：关键论断要能被验证或推翻，写明验证方式（压测/灰度对比/指标观测）。
- **风险应对**：具体到「触发条件 + 动作 + 责任方」，含降级与回滚预案；不写「加强监控」这类空话。
- **实施计划**：给时间区间并注明假设（人力、依赖、排期变化时的调整方式）；里程碑要能独立交付。

## 反模式（出现即返工）

| 反模式 | 症状 | 改法 |
|--------|------|------|
| 形容词化 | 「高性能」「海量数据」「高可用」 | 换成量化目标 + 口径（`quantification.md`） |
| 单边选型 | 只给一个方案或只写优点 | 补 ≥2 方案 + 代价 + 不该选的场景 |
| 章节空壳 | 章节齐全但每节一句话、"N/A" 占位 | 删掉不适用章节，其余写实 |
| 图话两说 | 图里有正文没解释的组件，或正文流程图上没有 | 图与正文互相补齐 |
| 空话风控 | 「加强监控」「做好兜底」 | 触发条件 + 动作 + 回滚步骤 |
| 不可验收 | 「提升用户体验」「性能明显改善」 | 给可测指标 + 验证方式 + 责任方 |
| 逃逸不确定性 | 把待定项写成「后续再定」散在正文 | 集中到「开放问题」并标默认倾向 |
| 过度设计 | 小需求套成本/容灾/压测全套 | 下浮档位（`templates.md`） |
| 假设隐身 | 用了未申明的假设（如"假设用户都在同一机房"） | 显式写进「假设与约束」，标待确认 |

## 评审检查清单（单一真源）

**唯一清单来源在本节。** 模板不内嵌清单；收尾时按下表取子集，再叠加 `focus-areas.md` 对应专题的必答项，生成在文末「评审检查清单」小节或口头收口。

| 档位 | 取用方式 |
|------|----------|
| 轻量 | 每类取最关键 1-2 条，合计 5-8 条 |
| 标准 | 每类 2-3 条 |
| 完整 | 全量 + 专题必答项 |

- **目标与范围**：目标可衡量；非目标（超出范畴的用例）已显式列出并给原因；范围与档位匹配，无过度设计。
- **架构合理性**：模块划分清晰、职责与数据所有权不重叠；系统边界明确；无未说明的单点故障。
- **量化与容量**：性能/容量/可用性目标有推导与口径；容量规划含规划期总量与扩容阈值；缓存策略覆盖读写路径与失效。
- **安全**：认证授权机制明确；数据加密与密钥管理；越权防护；敏感数据与日志脱敏。
- **可维护与可观测**：监控/日志/链路方案到位（完整档）；接手人能独立读懂；关键决策有记录（ADR）。
- **迁移与回滚**：数据迁移与兼容期有方案；回滚触发条件与步骤可执行；不可逆动作有审批与检查项。
- **测试与验收**：验收标准可验证（谁验、怎么验、阈值）；关键路径有测试策略（单测/集成/压测/灰度对比）。
- **风险控制**：主要风险已识别且应对可行；假设与不确定性显式化。

## 语言与可读性

- 全文**简体中文**；技术术语保留英文（API、RPC、Kafka），首现给中文释义。
- 语义式标题层级；章节长度与信息量匹配；重要结论用加粗或列表强调。
- 论证充分但克制：每节回答「为什么」而不是堆砌内容；非技术人员也能读懂核心思路。
